import 'package:flutter/material.dart';

class FolderApp extends StatelessWidget {
  const FolderApp({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return const SizedBox.shrink();
  }
}
